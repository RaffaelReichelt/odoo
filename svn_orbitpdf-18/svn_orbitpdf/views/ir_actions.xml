<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="act_report_xml_form_view" model="ir.ui.view">
        <field name="name">ir.actions.report.form</field>
        <field name="model">ir.actions.report</field>
        <field name="inherit_id" ref="base.act_report_xml_view"/>
        <field name="arch" type="xml">
            <field name="report_type" position="after">
                <field name="u_pdf_engine" invisible="report_type != 'qweb-pdf'" placeholder="(uses the default engine when empty)"/>
            </field>
            <field name="paperformat_id" position="attributes">
                <attribute name="placeholder">(uses the company format when empty)</attribute>
            </field>
        </field>
    </record>
    <record id="act_report_xml_list_view" model="ir.ui.view">
        <field name="name">ir.actions.report.list</field>
        <field name="model">ir.actions.report</field>
        <field name="inherit_id" ref="base.act_report_xml_view_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//list" position="attributes">
                <attribute name="multi_edit">1</attribute>
            </xpath>
            <field name="attachment" position="attributes">
                <attribute name="optional">hide</attribute>
            </field>
            <field name="attachment" position="after">
                <field name="paperformat_id" optional="show"/>
                <field name="u_pdf_engine" class="bg-100 text-center" decoration-info="u_pdf_engine != 'default'"/>
            </field>
        </field>
    </record>
</odoo>
